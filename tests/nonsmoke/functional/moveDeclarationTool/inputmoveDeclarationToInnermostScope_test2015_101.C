#define MAGIC_NUMBER 0;

void foobar()
   {
      int i;

   // The macro call that is part of an expression but used to terminate a statement, we need
   // to terminate with the macro call with a ";" (instead of it being in the macro definition).
      int abc = MAGIC_NUMBER

      int j;

      { i = 42; }
      
   }
