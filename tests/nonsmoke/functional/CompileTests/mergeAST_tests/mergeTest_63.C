// Example showing that we have to gather the statements in a block to build the delete list!
namespace X
   {
     typedef struct
        {
       // int __pos;
          int __state;
        } _G_fpos64_t;
#if 1
     typedef struct
        {
       // int __pos;
          int __state;
        } _G_fpos64;
#endif
   }

