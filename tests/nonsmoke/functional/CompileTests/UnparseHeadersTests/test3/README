In this test, a single source file includes two header files in
different scopes. One of the header files declares a class, the other
one - contains several nested scopes and declares a variable in the
most outer scope. Both header files are unparsed because a variable is
renamed in each of them.
