public class test2011_40
   {
     public void foo()
        {
       // A type that we could previously find as an implicit type.
          String s;

       // A type name that was previously an error to find because 
       // the implementation was too specific to the String type.
          ArithmeticException exception;
        }
   }
