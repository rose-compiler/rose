// This test code failed until the AST array type indexes were fixed to 
// be deleted properly when sharing array types from the type table.

#include <bits/c++config.h>
#include <bits/cpp_type_traits.h>
#include <cmath>

#include <istream>

#include <ostream>

int main()
   {
     return 0;
   }
