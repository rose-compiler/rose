int x;

struct process {
//  struct process *next;
//  pid_t pid;
//  char text[80];
    int status;
//  child_times_t ti;
//  struct timeval bgtime;
//  struct timeval endtime;
};

void foobar_1()
   {
  // typedef struct process *Process;
     struct process *p_foobar_1;

  // if ((((signed char) ((((__extension__ ({ union { __typeof(p_foobar_1->status) __in; int __i; } __u; __u.__in = (p_foobar_1->status); __u.__i; }))) & 0x7f) + 1) >> 1) > 0))
     if ((((signed char) ((((__extension__ ({ union { __typeof(p_foobar_1->status) __in; int __i; } __u; __u.__in = (p_foobar_1->status); __u.__i; }))) & 0x7f) + 1) >> 1) > 0)) 
        {
        }
   }

void foobar_2()
   {
  // typedef struct process *Process;
     struct process *p_foobar_2;

  // if ((((signed char) ((((__extension__ ({ union { __typeof(p_foobar_2->status) __in; int __i; } __u; __u.__in = (p_foobar_2->status); __u.__i; }))) & 0x7f) + 1) >> 1) > 0)) 
     if ((((signed char) ((((__extension__ ({ union { __typeof(p_foobar_2->status) __in; int __i; } __u; __u.__in = (p_foobar_2->status); __u.__i; }))) & 0x7f) + 1) >> 1) > 0)) 
        {
        }
   }

