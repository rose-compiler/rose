int bar(int a) {
   return a;
}

void foo() {
   int i=3;
   int j=8;
// bar (0);

// bar(i) ? bar(1) : bar(2);
// (i > 3) ? i : j;
   (i) ? -1 : j;

   for (i=0; i<5; i++);

// for (i=0; i<5; i++);
// while (0);
// i = 6;
}
