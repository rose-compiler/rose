// This test code demonstrates that the "static" modifier is not set for the initial declaration (in the unparsed code).
// we can have as many as we want of these.
static int scalar;
// static int scalar;

// This is the variable definition (initialized with a value) we can only have one of these.
static int scalar = 1;

// This is a redundant extern declaration.
// extern int scalar;
