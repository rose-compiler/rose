// #include <stddef.h>
// size_t size = sizeof(int) == 4 ? 4 : 0;

// int x;
// size_t size = sizeof(x) == 4 ? 4 : 0;

// size_t sizeInt = sizeof(int);

typedef unsigned long size_t;
// size_t size = sizeof(int) == sizeof(int) ? sizeof(int) : sizeof(int);

size_t size = sizeof(int) == 4 ? 4 : 0;
