

struct X
   {
     struct Y
        {
          one, two
        };
   };


struct Z
   {
  // Note that for C, types are always in global scope.
     struct YY
        {
          one, two
        };
   };


struct Y number;
