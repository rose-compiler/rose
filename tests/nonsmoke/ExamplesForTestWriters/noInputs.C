// A stupid example checker that doesn't ready any specimen.  A real checker would probably include some ROSE header files,
// and call some ROSE functions. ROSE unit testing would be a perfect example.
int
main() {
    // do nothing except pass
}
