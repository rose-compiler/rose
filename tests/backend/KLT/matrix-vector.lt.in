params(n,m)
data(A[0:n][0:m], flow-in)
data(B[0:m],      flow-in)
data(C[0:n],      flow-out)
loop-trees(
  loop(
    i, 0, n-1, parfor,
    stmt(C[i]=0;),
    loop(
      j, 0, m-1, reduction(C[i]),
      stmt(C[i]+=A[i][j]*B[j];)
    )
  )
)
