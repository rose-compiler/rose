class X
   {
     public:
          typedef X *Xstar;

      // Xstar xp;

      // X (int) {}
         X (int);

      // Calling the constructor through a typedef
      // X (float) { Xstar(0); }
   };


   
