class node
   {
     public:
       // int data;
       // node *next;
       // node(int i):data(i){}
       // node(int i);
       // void setNext(node* n){next = n;}
   };

// node* root;
void init()
   {
  // root = new node(1);
     node* root = new node;
   }
