#if 0
// Just a thought, but does this unparse correctly?
const float const* foobar_in_header (float* var_x);
#endif


struct X {};

typedef int (*foobar_function_pointer) (struct X *, struct X *,__const unsigned char **, __const unsigned char *, unsigned char **, int, int);
