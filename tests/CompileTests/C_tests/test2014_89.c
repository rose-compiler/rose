int mode[2], bits[2];

void foobar(int x, int y)
   {
  // int dmode, mode[2], bits[2];
     int len, ch, ret;
     int dmode, mode[2], bits[2];
     int *ch0, *ch1;
     int i, t, t2;

     bits[0] = 16;
     bits[1] = 32;
   }

