
int mail_run_foreground()
   {
     int status;
     int pid;

     switch (pid)
        {
          default:
//             do {} while (1);
               { return(pid == -1 ? -1 : ((((__extension__ (((union { __typeof(status) __in; int __i; }) { .__in = (status) }).__i))) & 0x7f) == 0) ? ((((__extension__ (((union { __typeof(status) __in; int __i; }) { .__in = (status) }).__i))) & 0xff00) >> 8) : 1); }
        }
   }


