

class Integer
{
};

class GenericGridData
   {
     public:
          int computedGeometry;
   };

class GenericGrid
   {
     public:
          inline const int & computedGeometry() const
             { return rcData->computedGeometry; }

          typedef GenericGridData RCData;
          RCData* rcData;
   };

