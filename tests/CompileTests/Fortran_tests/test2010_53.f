      program cg
      implicit none

      integer T_init, T_last
      parameter (T_init=1, T_last=3)

      character t_names(t_last)*8

	 t_names(t_init) = 'init'
      end                              ! end main


