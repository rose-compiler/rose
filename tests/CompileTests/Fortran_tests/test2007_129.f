      SUBROUTINE DGEMM ()
      INTEGER ZERO

      IF( ZERO .GT. ZERO )THEN
         IF( ZERO .EQ. ZERO )THEN
            DO 20, J = 1, N
   20       CONTINUE
         END IF
      END IF
      RETURN
      END
