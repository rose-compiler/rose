# The format of this configuration file is a binary file name, followed by a colon, followed the
# names of all the tests that should be run. The test names are defined in th testSampleBinaries.sh
# shell script.
#
# The binary file name can be either the full path, a partial path (full path with the first argument
# of testSampleBinaries.sh removed), just the file name, or the symbol '*'. They are searched in that
# order and the first entry that's found is the one that's used. If no entry is found then a built-in
# list of default tests are run (at this time, the default list is just the test "skip", which always
# succeeds).

*: 				parser disassembler assembler

ARPPRODUCTICON.exe: 		skip
PFE32.EXE:			parser disassembler
arm-ctrlaltdel:			parser disassembler
arm-nologin:			parser disassembler
arm-pivot_root:			parser disassembler
arm-poweroff:			parser disassembler
asm_code_samples_gcc.exe:	parser disassembler
bgl-helloworld-mpicc:		skip
bgl-helloworld-mpixlc:		skip
buffer2.bin:			parser disassembler
exefmt.exe:			parser disassembler
fnord.i386:			parser disassembler
fnord.ppc:			parser disassembler
i386-ctrlaltdel:		parser disassembler
i386-fsck.cramfs:		parser disassembler
i386-nologin:			parser disassembler
i386-pivot_root:		parser disassembler
i386-poweroff:			parser disassembler
i686-test1.O0.bin:		parser disassembler
i686-test1.O3.bin:		parser disassembler
libm-2.3.6.so:			parser disassembler
pocketputty.exe:		parser disassembler
pocketputty2.exe:		parser disassembler
proxycfg.exe:			parser disassembler
puttytel.exe:			parser disassembler

