# Test configuration file (see scripts/test_harness.pl for details).

cmd = ./rot13 --encode <${BINARY_SAMPLES}/${INPUT} | ./rot13 --decode >${TEMP_FILE_0}
cmd = diff ${BINARY_SAMPLES}/${INPUT} ${TEMP_FILE_0}
