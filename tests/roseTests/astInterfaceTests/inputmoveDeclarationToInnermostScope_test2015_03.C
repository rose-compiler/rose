 int n,m,mits;
// double dx,dy;

void initialize()
{
  int i,j, xx,yy;

// dx = 2.0 / (n-1);
// dy = 2.0 / (m-1);

  for (i=0;i<n;i++)
    for (j=0; j<m; j++ )    
    {
      xx =(int)( -1.0 + 2.0 * (i-1));    
    }   
}
