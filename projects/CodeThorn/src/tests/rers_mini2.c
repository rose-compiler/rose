#include <stdio.h> 
#include <assert.h>

// MS: 

        // inputs
        int f=5;

int calculate_output(int input) {
  if(f==5)
	assert(0);
  return -1;
}

int main()
{
  int input;
  int output;
  output = calculate_output(input);
  return 0;
}
