param(n, unsigned)
scalar(offset, float)
data(a, float, section(1, 0, n), openacc(copy))
region(
  openacc(nbr_gang,nbr_worker,nbr_vector),
  loop(
    i, 0, n, openacc(gang,worker,vector),
    stmt(a[i]+=offset;)
  )
)
