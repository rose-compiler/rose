/* Disabled because the test harness currently can't handle input that doesn't include mpi.h at all */

int SOURCE_DETERMINISM = 1;
int TAG_DETERMINISM = 1;
int FUNCTION_DETERMINISM = 1;

int main(int argc, char **argv) {
  return 0;
}
