#include <stdio.h>
#include "mpi.h"

int main(argc,argv)
int argc;
char **argv;
{
  int rank;
  int size;
  MPI_Init(&argc,&argv);
  MPI_Comm_size(&ompi_mpi_comm_world,&size);
  MPI_Comm_rank(&ompi_mpi_comm_world,&rank);
  MPI_Finalize();
  return 0;
}
