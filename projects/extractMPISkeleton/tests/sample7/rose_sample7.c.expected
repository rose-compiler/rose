#include <stdlib.h> 

int main()
{
  int x = 5;
  int y = 0;
// make true 60% of time.
  
#pragma skel condition prob(6/10)
  if ((x % 2 , ((double )(rand())) / RAND_MAX < 6 / 10)) {
    x += 5;
  }
  else {
    x = 0;
  }
  return x;
}
