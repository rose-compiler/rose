{
  std::vector<VirtualCFG::CFGEdge> paletteEdges# = ($0).$s1();
  for (std::vector<VirtualCFG::CFGEdge>::const_iterator
         paletteEdgeIterator# = paletteEdges#.begin();
       paletteEdgeIterator# != paletteEdges#.end();
       ++paletteEdgeIterator#) {
    VirtualCFG::CFGNode $s3 = paletteEdgeIterator#->$s2();
    @
  }
}

