// vim: syn=cpp
{
  std::vector<SgNode*> paletteChildren# =
    ($0)->get_traversalSuccessorContainer();
  for (std::vector<SgNode*>::const_iterator paletteChildIterator# =
         paletteChildren#.begin();
       paletteChildIterator# != paletteChildren#.end();
       ++paletteChildIterator#) {
    SgNode* $s1 = *paletteChildIterator#;
    @
  }
}

