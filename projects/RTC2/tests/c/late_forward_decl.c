#include <stdlib.h>

typedef struct node
{
  struct node* next;
} node_t;

void insert_node(node_t* insert, node_t* after)
{
  // insert <- second
  if (insert->next) {}
}

unsigned int fn1(unsigned int* input)
{
  return *input;
}

int main()
{
  return 0;
}
