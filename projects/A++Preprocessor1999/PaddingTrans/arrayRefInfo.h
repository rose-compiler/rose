//-*-Mode: C++;-*-
#ifndef _arrayRefInfo_h_
#define _arrayRefInfo_h_

// CW: !!! ATTENTION !!!
// CW: not yet implemented!!!
// Should store all data which is needed to process array references
// The data must be sufficient to identify the A++ array variable
// which is referenced, check whether two references are uniformly
// generated, and generate the constant vector which is used in the
// arrayInfo class.
class arrayRefInfo
{
  public:
  
  private:
};

#endif
