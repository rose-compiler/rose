1) 2x1 ANALYSIS in : main-support.C
   DFI_STORE doit(ANALYSIS)(void *);
   DFI_STORE perform_pag_analysis(ANALYSIS)(SgProject *root,char* output, bool noresult)

2) 2x2 CARRIER_TYPE in : StatementAttributeTraversal.C
   carrier_printfunc(CARRIER_TYPE)((carrier_type_o(CARRIER_TYPE))get_statement_pre_info(store, stmt));
   DONE: moved the relevant functions to main-support, modified the interface, and turned all
         AttributeTraversal classes into templates (with type argument DFI_STORE_TYPE)

3) AstAttribute @ SgBasicBlock
