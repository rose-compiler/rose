--- Shape Analysis ---

* make 
  requires the following environment variables being set:
  PIG (refering to the binary of PIG)
  PAGDIR (the directory of PAG)
  LD_LIBRARY_PATH (must include the ROSE 'lib' directory);
    on b3 that's /usr/local/mstools/lib/rose

* make check
  uses test files in 'tests'
