SET

DOMAIN
 // Bound is the same as cp_Const, which is included automatically.
 // Bound = flat(snum)
 // Interval = Bound * Bound
    Interval = cp_Const * cp_Const
    fState = VariableId -> Interval
    IntervalStatePair = Interval * State
    BoolL = flat(bool)
    State = lift(fState)
