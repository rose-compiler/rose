#!/bin/sh
#
# Bootstraps PDFlib-Lite-6.0.2rv. More specifically, this script
# builds a modified "configure" script which permits building
# the library in a separate build tree. (See 'configure.in')
#
# The original "configure" and "configure.in" files packaged with
# PDFlib-Lite-6.0.2 have been preserved in "configure.orig" and
# "configure.in.orig", respectively.
#

aclocal -I ./config || exit 1
autoconf || exit 1

# eof
