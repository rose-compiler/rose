include_rules

# This directory no longer compiles correctly
ifeq (0,1)

    ifneq (@(WITH_SQLITE),no)
	run $(librose_compile) sideEffect.C
	run $(public_header) sideEffect.h SqliteDatabaseGraph.h
    endif

endif
