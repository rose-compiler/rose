# Control file for ROSE policies

# This file appears to be generated by "make dist" in this directory rather than
# the annotationLanguageParser directory, at least on some platforms.  The file
# contains TAB characters inserted by lex/yacc over which we have no control.
language.tab.h: disable NoTabCharacters
