// This file doesn't need any functions, since it is only used when binary
// analysis support is turned off and so none of the functions in
// objdumpDisassembler.C are referenced.  It only exists because the Mac OS X
// linker doesn't allow an archive to have no files.
