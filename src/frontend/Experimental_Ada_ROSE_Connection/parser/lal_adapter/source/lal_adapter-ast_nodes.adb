with Libadalang.Common;

package body LAL_Adapter.AST_Nodes is

   package LALC renames Libadalang.Common;

   ------------
   -- Exported:
   ------------

   procedure Process (This : in Ada_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Basic_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Single_Tok_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Id) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Identifier) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abort_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abort_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abort_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Simple_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abort_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Basic_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Classic_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Formal_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_Formal_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_State_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_State_Decl_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Abstract_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Composite_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Accept_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Accept_Stmt_With_Stmts) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Access_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Access_To_Subp_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Basic_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aggregate_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aliased_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aliased_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aliased_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in All_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in All_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in All_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Allocator) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Anonymous_Expr_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Type_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Anonymous_type) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Type_Access_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Anonymous_Type_Access_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Anonymous_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Array_Indices) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Array_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aspect_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aspect_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Aspect_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Assign_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in At_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Attribute_Def_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Attribute_Ref) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Formal_Param_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Formal_Param_Holder) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Loop_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Package_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Record_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Body_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Subp_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Subp_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Base_Subtype_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Block_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Begin_Block) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Bin_Op) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Body_Stub) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Box_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Bracket_Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Delta_Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Bracket_Delta_Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Call_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Call_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Cond_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Case_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Case_Expr_Alternative) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Case_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Case_Stmt_Alternative) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Char_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Classwide_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Compilation_Unit) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Component_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Component_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Component_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Component_List) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Concrete_Formal_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constant_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constant_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constant_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constrained_Array_Indices) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subtype_Indication) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constrained_Subtype_Indication) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Contract_Case_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Contract_Cases) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Real_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Decimal_Fixed_Point_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Decl_Block) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Decl_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Declarative_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Defining_Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Delay_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Delta_Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Derived_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Digits_Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discrete_Base_Subtype_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discrete_Subtype_Indication) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discrete_Subtype_Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discriminant_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discriminant_Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discriminant_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Discriminant_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Dotted_Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Elsif_Expr_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Elsif_Stmt_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in End_Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Entry_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Entry_Completion_Formal_Params) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Entry_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Entry_Index_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Entry_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Enum_Lit_Synth_Type_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Enum_Literal_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Enum_Rep_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Enum_Subp_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Enum_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Error_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Error_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Exception_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Exception_Handler) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Exit_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Explicit_Deref) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Expr_function) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Extended_Return_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Object_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Extended_Return_Stmt_Object_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Floating_Point_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Loop_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in For_Loop_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in For_Loop_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in For_Loop_Var_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Formal_Discrete_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal_Obj_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal_package) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Formal_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Instantiation) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Package_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Package_Instantiation) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Package_Internal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Renaming_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Package_Renaming_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Subp_Instantiation) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Subp_Internal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Generic_Subp_Renaming_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Goto_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Handled_Stmts) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in If_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in If_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Incomplete_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Incomplete_Tagged_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Index_Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Num_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Int_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Kind) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Kind_limited) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Kind_protected) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Kind_synchronized) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Kind_task) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Interface_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Iter_type) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Iter_Type_in) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Iter_Type_of) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Iterated_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Known_Discriminant_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Label) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Label_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Library_Item) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Limited_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Limited_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Limited_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Loop_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Membership_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mod_Int_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mode) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mode_Default) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mode_in) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mode_In_out) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Mode_out) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Multi_Abstract_State_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Multi_Dim_Array_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Named_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Named_Stmt_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Not_null) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Not_Null_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Not_Null_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Component_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Record_Aggregate) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Record_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Null_Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Number_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_abs) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_and) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_And_then) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Concat) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Div) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Double_Dot) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Eq) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Gt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Gte) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_in) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Lt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Lte) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Minus) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_mod) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Mult) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Neq) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_not) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Not_in) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_or) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Or_else) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Plus) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_Pow) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_rem) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Op_xor) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Ordinary_Fixed_Point_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Others_Designator) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Overriding_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Overriding_Not_overriding) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Overriding_overriding) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Overriding_Unspecified) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Package_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Package_Body_Stub) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Package_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Package_Renaming_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Param_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Param_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Params) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Paren_Abstract_State_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Paren_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Pragma_Argument_Assoc) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Pragma_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Private_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Private_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Private_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Private_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Private_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Body_Stub) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Protected_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Public_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Qual_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Quantified_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Quantifier) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Quantifier_all) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Quantifier_some) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Raise_Expr) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Raise_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Range_Constraint) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Range_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Real_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Record_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Record_Rep_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Record_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Relation_Op) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Renaming_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Requeue_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Return_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Reverse_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Reverse_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Reverse_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Select_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Select_When_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Signed_Int_Type_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Single_Protected_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Single_Task_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Task_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Single_Task_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in String_Literal) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Body_Stub) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Kind) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Kind_function) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Kind_procedure) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Renaming_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subp_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subtype_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Subunit) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Synchronized_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Synchronized_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Synchronized_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Synth_Anonymous_Type_Decl) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Synthetic_Renaming_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Tagged_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Tagged_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Tagged_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Target_Name) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Task_body) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Task_Body_Stub) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Task_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Terminate_Alternative) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Type_Access_Def) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Un_Op) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Unconstrained_Array_Index) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Unconstrained_Array_Indices) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Unknown_Discriminant_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Until_Node) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Until_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Until_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Update_Attribute_Ref) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Use_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Use_Package_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Use_Type_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Variant) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in Variant_Part) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in While_Loop_Spec) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in While_Loop_Stmt) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in With_Clause) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in With_private) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in With_Private_Absent) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   ------------
   -- Exported:
   ------------

   overriding procedure Process (This : in With_Private_Present) is
   begin

      raise Program_Error with "Unimplemented procedure Process";
   end Process;

   -----------
   -- Private:
   -----------
   function To_Ada_Node
     (This : in LAL.Ada_Node'Class)
      return Ada_Node_Access_Class is
   begin -- To_Ada_Node
      case LAL.Kind (This) is
         when LALC.Ada_Abort_Absent =>
            return new Abort_Absent;
         when LALC.Ada_Abort_Present =>
            return new Abort_Present;
         when LALC.Ada_Abort_Stmt =>
            return new Abort_Stmt;
         when LALC.Ada_Abstract_Absent =>
            return new Abstract_Absent;
         when LALC.Ada_Abstract_Formal_Subp_Decl =>
            return new Abstract_Formal_Subp_Decl;
         when LALC.Ada_Abstract_Present =>
            return new Abstract_Present;
         when LALC.Ada_Abstract_State_Decl =>
            return new Abstract_State_Decl;
         when LALC.Ada_Abstract_State_Decl_Expr =>
            return new Abstract_State_Decl_Expr;
         when LALC.Ada_Abstract_State_Decl_List =>
            return new Abstract_State_Decl_List;
         when LALC.Ada_Abstract_Subp_Decl =>
            return new Abstract_Subp_Decl;
         when LALC.Ada_Accept_Stmt =>
            return new Accept_Stmt;
         when LALC.Ada_Accept_Stmt_With_Stmts =>
            return new Accept_Stmt_With_Stmts;
         when LALC.Ada_Access_To_Subp_Def =>
            return new Access_To_Subp_Def;
         when LALC.Ada_Ada_Node_List =>
            return new Ada_Node_List;
         when LALC.Ada_Aggregate =>
            return new Aggregate;
         when LALC.Ada_Aggregate_Assoc =>
            return new Aggregate_Assoc;
         when LALC.Ada_Aliased_Absent =>
            return new Aliased_Absent;
         when LALC.Ada_Aliased_Present =>
            return new Aliased_Present;
         when LALC.Ada_All_Absent =>
            return new All_Absent;
         when LALC.Ada_All_Present =>
            return new All_Present;
         when LALC.Ada_Allocator =>
            return new Allocator;
         when LALC.Ada_Alternatives_List =>
            return new Alternatives_List;
         when LALC.Ada_Anonymous_Expr_Decl =>
            return new Anonymous_Expr_Decl;
         when LALC.Ada_Anonymous_Type =>
            return new Anonymous_Type;
         when LALC.Ada_Anonymous_Type_Access_Def =>
            return new Anonymous_Type_Access_Def;
         when LALC.Ada_Anonymous_Type_Decl =>
            return new Anonymous_Type_Decl;
         when LALC.Ada_Array_Type_Def =>
            return new Array_Type_Def;
         when LALC.Ada_Aspect_Assoc =>
            return new Aspect_Assoc;
         when LALC.Ada_Aspect_Assoc_List =>
            return new Aspect_Assoc_List;
         when LALC.Ada_Aspect_Spec =>
            return new Aspect_Spec;
         when LALC.Ada_Assign_Stmt =>
            return new Assign_Stmt;
         when LALC.Ada_Assoc_List =>
            return new Assoc_List;
         when LALC.Ada_At_Clause =>
            return new At_Clause;
         when LALC.Ada_Attribute_Def_Clause =>
            return new Attribute_Def_Clause;
         when LALC.Ada_Attribute_Ref =>
            return new Attribute_Ref;
         when LALC.Ada_Base_Assoc_List =>
            return new Base_Assoc_List;
         when LALC.Ada_Basic_Decl_List =>
            return new Basic_Decl_List;
         when LALC.Ada_Begin_Block =>
            return new Begin_Block;
         when LALC.Ada_Bin_Op =>
            return new Bin_Op;
         when LALC.Ada_Box_Expr =>
            return new Box_Expr;
         when LALC.Ada_Bracket_Aggregate =>
            return new Bracket_Aggregate;
         when LALC.Ada_Bracket_Delta_Aggregate =>
            return new Bracket_Delta_Aggregate;
         when LALC.Ada_Call_Expr =>
            return new Call_Expr;
         when LALC.Ada_Call_Stmt =>
            return new Call_Stmt;
         when LALC.Ada_Case_Expr =>
            return new Case_Expr;
         when LALC.Ada_Case_Expr_Alternative =>
            return new Case_Expr_Alternative;
         when LALC.Ada_Case_Expr_Alternative_List =>
            return new Case_Expr_Alternative_List;
         when LALC.Ada_Case_Stmt =>
            return new Case_Stmt;
         when LALC.Ada_Case_Stmt_Alternative =>
            return new Case_Stmt_Alternative;
         when LALC.Ada_Case_Stmt_Alternative_List =>
            return new Case_Stmt_Alternative_List;
         when LALC.Ada_Char_Literal =>
            return new Char_Literal;
         when LALC.Ada_Classwide_Type_Decl =>
            return new Classwide_Type_Decl;
         when LALC.Ada_Compilation_Unit =>
            return new Compilation_Unit;
         when LALC.Ada_Compilation_Unit_List =>
            return new Compilation_Unit_List;
         when LALC.Ada_Component_Clause =>
            return new Component_Clause;
         when LALC.Ada_Component_Decl =>
            return new Component_Decl;
         when LALC.Ada_Component_Def =>
            return new Component_Def;
         when LALC.Ada_Component_List =>
            return new Component_List;
         when LALC.Ada_Concrete_Formal_Subp_Decl =>
            return new Concrete_Formal_Subp_Decl;
         when LALC.Ada_Constant_Absent =>
            return new Constant_Absent;
         when LALC.Ada_Constant_Present =>
            return new Constant_Present;
         when LALC.Ada_Constrained_Array_Indices =>
            return new Constrained_Array_Indices;
         when LALC.Ada_Constrained_Subtype_Indication =>
            return new Constrained_Subtype_Indication;
         when LALC.Ada_Constraint_List =>
            return new Constraint_List;
         when LALC.Ada_Contract_Case_Assoc =>
            return new Contract_Case_Assoc;
         when LALC.Ada_Contract_Case_Assoc_List =>
            return new Contract_Case_Assoc_List;
         when LALC.Ada_Contract_Cases =>
            return new Contract_Cases;
         when LALC.Ada_Decimal_Fixed_Point_Def =>
            return new Decimal_Fixed_Point_Def;
         when LALC.Ada_Decl_Block =>
            return new Decl_Block;
         when LALC.Ada_Decl_Expr =>
            return new Decl_Expr;
         when LALC.Ada_Decl_List =>
            return new Decl_List;
         when LALC.Ada_Declarative_Part =>
            return new Declarative_Part;
         when LALC.Ada_Defining_Name =>
            return new Defining_Name;
         when LALC.Ada_Defining_Name_List =>
            return new Defining_Name_List;
         when LALC.Ada_Delay_Stmt =>
            return new Delay_Stmt;
         when LALC.Ada_Delta_Aggregate =>
            return new Delta_Aggregate;
         when LALC.Ada_Delta_Constraint =>
            return new Delta_Constraint;
         when LALC.Ada_Derived_Type_Def =>
            return new Derived_Type_Def;
         when LALC.Ada_Digits_Constraint =>
            return new Digits_Constraint;
         when LALC.Ada_Discrete_Base_Subtype_Decl =>
            return new Discrete_Base_Subtype_Decl;
         when LALC.Ada_Discrete_Subtype_Indication =>
            return new Discrete_Subtype_Indication;
         when LALC.Ada_Discrete_Subtype_Name =>
            return new Discrete_Subtype_Name;
         when LALC.Ada_Discriminant_Assoc =>
            return new Discriminant_Assoc;
         when LALC.Ada_Discriminant_Choice_List =>
            return new Discriminant_Choice_List;
         when LALC.Ada_Discriminant_Constraint =>
            return new Discriminant_Constraint;
         when LALC.Ada_Discriminant_Spec =>
            return new Discriminant_Spec;
         when LALC.Ada_Discriminant_Spec_List =>
            return new Discriminant_Spec_List;
         when LALC.Ada_Dotted_Name =>
            return new Dotted_Name;
         when LALC.Ada_Elsif_Expr_Part =>
            return new Elsif_Expr_Part;
         when LALC.Ada_Elsif_Expr_Part_List =>
            return new Elsif_Expr_Part_List;
         when LALC.Ada_Elsif_Stmt_Part =>
            return new Elsif_Stmt_Part;
         when LALC.Ada_Elsif_Stmt_Part_List =>
            return new Elsif_Stmt_Part_List;
         when LALC.Ada_End_Name =>
            return new End_Name;
         when LALC.Ada_Entry_Body =>
            return new Entry_Body;
         when LALC.Ada_Entry_Completion_Formal_Params =>
            return new Entry_Completion_Formal_Params;
         when LALC.Ada_Entry_Decl =>
            return new Entry_Decl;
         when LALC.Ada_Entry_Index_Spec =>
            return new Entry_Index_Spec;
         when LALC.Ada_Entry_Spec =>
            return new Entry_Spec;
         when LALC.Ada_Enum_Lit_Synth_Type_Expr =>
            return new Enum_Lit_Synth_Type_Expr;
         when LALC.Ada_Enum_Literal_Decl =>
            return new Enum_Literal_Decl;
         when LALC.Ada_Enum_Literal_Decl_List =>
            return new Enum_Literal_Decl_List;
         when LALC.Ada_Enum_Rep_Clause =>
            return new Enum_Rep_Clause;
         when LALC.Ada_Enum_Subp_Spec =>
            return new Enum_Subp_Spec;
         when LALC.Ada_Enum_Type_Def =>
            return new Enum_Type_Def;
         when LALC.Ada_Error_Decl =>
            return new Error_Decl;
         when LALC.Ada_Error_Stmt =>
            return new Error_Stmt;
         when LALC.Ada_Exception_Decl =>
            return new Exception_Decl;
         when LALC.Ada_Exception_Handler =>
            return new Exception_Handler;
         when LALC.Ada_Exit_Stmt =>
            return new Exit_Stmt;
         when LALC.Ada_Explicit_Deref =>
            return new Explicit_Deref;
         when LALC.Ada_Expr_Alternatives_List =>
            return new Expr_Alternatives_List;
         when LALC.Ada_Expr_Function =>
            return new Expr_Function;
         when LALC.Ada_Extended_Return_Stmt =>
            return new Extended_Return_Stmt;
         when LALC.Ada_Extended_Return_Stmt_Object_Decl =>
            return new Extended_Return_Stmt_Object_Decl;
         when LALC.Ada_Floating_Point_Def =>
            return new Floating_Point_Def;
         when LALC.Ada_For_Loop_Spec =>
            return new For_Loop_Spec;
         when LALC.Ada_For_Loop_Stmt =>
            return new For_Loop_Stmt;
         when LALC.Ada_For_Loop_Var_Decl =>
            return new For_Loop_Var_Decl;
         when LALC.Ada_Formal_Discrete_Type_Def =>
            return new Formal_Discrete_Type_Def;
         when LALC.Ada_Generic_Formal_Obj_Decl =>
            return new Generic_Formal_Obj_Decl;
         when LALC.Ada_Generic_Formal_Package =>
            return new Generic_Formal_Package;
         when LALC.Ada_Generic_Formal_Part =>
            return new Generic_Formal_Part;
         when LALC.Ada_Generic_Formal_Subp_Decl =>
            return new Generic_Formal_Subp_Decl;
         when LALC.Ada_Generic_Formal_Type_Decl =>
            return new Generic_Formal_Type_Decl;
         when LALC.Ada_Generic_Package_Decl =>
            return new Generic_Package_Decl;
         when LALC.Ada_Generic_Package_Instantiation =>
            return new Generic_Package_Instantiation;
         when LALC.Ada_Generic_Package_Internal =>
            return new Generic_Package_Internal;
         when LALC.Ada_Generic_Package_Renaming_Decl =>
            return new Generic_Package_Renaming_Decl;
         when LALC.Ada_Generic_Subp_Decl =>
            return new Generic_Subp_Decl;
         when LALC.Ada_Generic_Subp_Instantiation =>
            return new Generic_Subp_Instantiation;
         when LALC.Ada_Generic_Subp_Internal =>
            return new Generic_Subp_Internal;
         when LALC.Ada_Generic_Subp_Renaming_Decl =>
            return new Generic_Subp_Renaming_Decl;
         when LALC.Ada_Goto_Stmt =>
            return new Goto_Stmt;
         when LALC.Ada_Handled_Stmts =>
            return new Handled_Stmts;
         when LALC.Ada_Identifier =>
            return new Identifier;
         when LALC.Ada_If_Expr =>
            return new If_Expr;
         when LALC.Ada_If_Stmt =>
            return new If_Stmt;
         when LALC.Ada_Incomplete_Tagged_Type_Decl =>
            return new Incomplete_Tagged_Type_Decl;
         when LALC.Ada_Incomplete_Type_Decl =>
            return new Incomplete_Type_Decl;
         when LALC.Ada_Index_Constraint =>
            return new Index_Constraint;
         when LALC.Ada_Int_Literal =>
            return new Int_Literal;
         when LALC.Ada_Interface_Kind_Limited =>
            return new Interface_Kind_Limited;
         when LALC.Ada_Interface_Kind_Protected =>
            return new Interface_Kind_Protected;
         when LALC.Ada_Interface_Kind_Synchronized =>
            return new Interface_Kind_Synchronized;
         when LALC.Ada_Interface_Kind_Task =>
            return new Interface_Kind_Task;
         when LALC.Ada_Interface_Type_Def =>
            return new Interface_Type_Def;
         when LALC.Ada_Iter_Type_In =>
            return new Iter_Type_In;
         when LALC.Ada_Iter_Type_Of =>
            return new Iter_Type_Of;
         when LALC.Ada_Iterated_Assoc =>
            return new Iterated_Assoc;
         when LALC.Ada_Known_Discriminant_Part =>
            return new Known_Discriminant_Part;
         when LALC.Ada_Label =>
            return new Label;
         when LALC.Ada_Label_Decl =>
            return new Label_Decl;
         when LALC.Ada_Library_Item =>
            return new Library_Item;
         when LALC.Ada_Limited_Absent =>
            return new Limited_Absent;
         when LALC.Ada_Limited_Present =>
            return new Limited_Present;
         when LALC.Ada_Loop_Stmt =>
            return new Loop_Stmt;
         when LALC.Ada_Membership_Expr =>
            return new Membership_Expr;
         when LALC.Ada_Mod_Int_Type_Def =>
            return new Mod_Int_Type_Def;
         when LALC.Ada_Mode_Default =>
            return new Mode_Default;
         when LALC.Ada_Mode_In =>
            return new Mode_In;
         when LALC.Ada_Mode_In_Out =>
            return new Mode_In_Out;
         when LALC.Ada_Mode_Out =>
            return new Mode_Out;
         when LALC.Ada_Multi_Abstract_State_Decl =>
            return new Multi_Abstract_State_Decl;
         when LALC.Ada_Multi_Dim_Array_Assoc =>
            return new Multi_Dim_Array_Assoc;
         when LALC.Ada_Name_List =>
            return new Name_List;
         when LALC.Ada_Named_Stmt =>
            return new Named_Stmt;
         when LALC.Ada_Named_Stmt_Decl =>
            return new Named_Stmt_Decl;
         when LALC.Ada_Not_Null_Absent =>
            return new Not_Null_Absent;
         when LALC.Ada_Not_Null_Present =>
            return new Not_Null_Present;
         when LALC.Ada_Null_Component_Decl =>
            return new Null_Component_Decl;
         when LALC.Ada_Null_Literal =>
            return new Null_Literal;
         when LALC.Ada_Null_Record_Aggregate =>
            return new Null_Record_Aggregate;
         when LALC.Ada_Null_Record_Def =>
            return new Null_Record_Def;
         when LALC.Ada_Null_Stmt =>
            return new Null_Stmt;
         when LALC.Ada_Null_Subp_Decl =>
            return new Null_Subp_Decl;
         when LALC.Ada_Number_Decl =>
            return new Number_Decl;
         when LALC.Ada_Object_Decl =>
            return new Object_Decl;
         when LALC.Ada_Op_Abs =>
            return new Op_Abs;
         when LALC.Ada_Op_And =>
            return new Op_And;
         when LALC.Ada_Op_And_Then =>
            return new Op_And_Then;
         when LALC.Ada_Op_Concat =>
            return new Op_Concat;
         when LALC.Ada_Op_Div =>
            return new Op_Div;
         when LALC.Ada_Op_Double_Dot =>
            return new Op_Double_Dot;
         when LALC.Ada_Op_Eq =>
            return new Op_Eq;
         when LALC.Ada_Op_Gt =>
            return new Op_Gt;
         when LALC.Ada_Op_Gte =>
            return new Op_Gte;
         when LALC.Ada_Op_In =>
            return new Op_In;
         when LALC.Ada_Op_Lt =>
            return new Op_Lt;
         when LALC.Ada_Op_Lte =>
            return new Op_Lte;
         when LALC.Ada_Op_Minus =>
            return new Op_Minus;
         when LALC.Ada_Op_Mod =>
            return new Op_Mod;
         when LALC.Ada_Op_Mult =>
            return new Op_Mult;
         when LALC.Ada_Op_Neq =>
            return new Op_Neq;
         when LALC.Ada_Op_Not =>
            return new Op_Not;
         when LALC.Ada_Op_Not_In =>
            return new Op_Not_In;
         when LALC.Ada_Op_Or =>
            return new Op_Or;
         when LALC.Ada_Op_Or_Else =>
            return new Op_Or_Else;
         when LALC.Ada_Op_Plus =>
            return new Op_Plus;
         when LALC.Ada_Op_Pow =>
            return new Op_Pow;
         when LALC.Ada_Op_Rem =>
            return new Op_Rem;
         when LALC.Ada_Op_Xor =>
            return new Op_Xor;
         when LALC.Ada_Ordinary_Fixed_Point_Def =>
            return new Ordinary_Fixed_Point_Def;
         when LALC.Ada_Others_Designator =>
            return new Others_Designator;
         when LALC.Ada_Overriding_Not_Overriding =>
            return new Overriding_Not_Overriding;
         when LALC.Ada_Overriding_Overriding =>
            return new Overriding_Overriding;
         when LALC.Ada_Overriding_Unspecified =>
            return new Overriding_Unspecified;
         when LALC.Ada_Package_Body =>
            return new Package_Body;
         when LALC.Ada_Package_Body_Stub =>
            return new Package_Body_Stub;
         when LALC.Ada_Package_Decl =>
            return new Package_Decl;
         when LALC.Ada_Package_Renaming_Decl =>
            return new Package_Renaming_Decl;
         when LALC.Ada_Param_Assoc =>
            return new Param_Assoc;
         when LALC.Ada_Param_Spec =>
            return new Param_Spec;
         when LALC.Ada_Param_Spec_List =>
            return new Param_Spec_List;
         when LALC.Ada_Params =>
            return new Params;
         when LALC.Ada_Paren_Abstract_State_Decl =>
            return new Paren_Abstract_State_Decl;
         when LALC.Ada_Paren_Expr =>
            return new Paren_Expr;
         when LALC.Ada_Parent_List =>
            return new Parent_List;
         when LALC.Ada_Pragma_Argument_Assoc =>
            return new Pragma_Argument_Assoc;
         when LALC.Ada_Pragma_Node =>
            return new Pragma_Node;
         when LALC.Ada_Pragma_Node_List =>
            return new Pragma_Node_List;
         when LALC.Ada_Private_Absent =>
            return new Private_Absent;
         when LALC.Ada_Private_Part =>
            return new Private_Part;
         when LALC.Ada_Private_Present =>
            return new Private_Present;
         when LALC.Ada_Private_Type_Def =>
            return new Private_Type_Def;
         when LALC.Ada_Protected_Absent =>
            return new Protected_Absent;
         when LALC.Ada_Protected_Body =>
            return new Protected_Body;
         when LALC.Ada_Protected_Body_Stub =>
            return new Protected_Body_Stub;
         when LALC.Ada_Protected_Def =>
            return new Protected_Def;
         when LALC.Ada_Protected_Present =>
            return new Protected_Present;
         when LALC.Ada_Protected_Type_Decl =>
            return new Protected_Type_Decl;
         when LALC.Ada_Public_Part =>
            return new Public_Part;
         when LALC.Ada_Qual_Expr =>
            return new Qual_Expr;
         when LALC.Ada_Quantified_Expr =>
            return new Quantified_Expr;
         when LALC.Ada_Quantifier_All =>
            return new Quantifier_All;
         when LALC.Ada_Quantifier_Some =>
            return new Quantifier_Some;
         when LALC.Ada_Raise_Expr =>
            return new Raise_Expr;
         when LALC.Ada_Raise_Stmt =>
            return new Raise_Stmt;
         when LALC.Ada_Range_Constraint =>
            return new Range_Constraint;
         when LALC.Ada_Range_Spec =>
            return new Range_Spec;
         when LALC.Ada_Real_Literal =>
            return new Real_Literal;
         when LALC.Ada_Record_Def =>
            return new Record_Def;
         when LALC.Ada_Record_Rep_Clause =>
            return new Record_Rep_Clause;
         when LALC.Ada_Record_Type_Def =>
            return new Record_Type_Def;
         when LALC.Ada_Relation_Op =>
            return new Relation_Op;
         when LALC.Ada_Renaming_Clause =>
            return new Renaming_Clause;
         when LALC.Ada_Requeue_Stmt =>
            return new Requeue_Stmt;
         when LALC.Ada_Return_Stmt =>
            return new Return_Stmt;
         when LALC.Ada_Reverse_Absent =>
            return new Reverse_Absent;
         when LALC.Ada_Reverse_Present =>
            return new Reverse_Present;
         when LALC.Ada_Select_Stmt =>
            return new Select_Stmt;
         when LALC.Ada_Select_When_Part =>
            return new Select_When_Part;
         when LALC.Ada_Select_When_Part_List =>
            return new Select_When_Part_List;
         when LALC.Ada_Signed_Int_Type_Def =>
            return new Signed_Int_Type_Def;
         when LALC.Ada_Single_Protected_Decl =>
            return new Single_Protected_Decl;
         when LALC.Ada_Single_Task_Decl =>
            return new Single_Task_Decl;
         when LALC.Ada_Single_Task_Type_Decl =>
            return new Single_Task_Type_Decl;
         when LALC.Ada_Stmt_List =>
            return new Stmt_List;
         when LALC.Ada_String_Literal =>
            return new String_Literal;
         when LALC.Ada_Subp_Body =>
            return new Subp_Body;
         when LALC.Ada_Subp_Body_Stub =>
            return new Subp_Body_Stub;
         when LALC.Ada_Subp_Decl =>
            return new Subp_Decl;
         when LALC.Ada_Subp_Kind_Function =>
            return new Subp_Kind_Function;
         when LALC.Ada_Subp_Kind_Procedure =>
            return new Subp_Kind_Procedure;
         when LALC.Ada_Subp_Renaming_Decl =>
            return new Subp_Renaming_Decl;
         when LALC.Ada_Subp_Spec =>
            return new Subp_Spec;
         when LALC.Ada_Subtype_Decl =>
            return new Subtype_Decl;
         when LALC.Ada_Subtype_Indication =>
            return new Subtype_Indication;
         when LALC.Ada_Subunit =>
            return new Subunit;
         when LALC.Ada_Synchronized_Absent =>
            return new Synchronized_Absent;
         when LALC.Ada_Synchronized_Present =>
            return new Synchronized_Present;
         when LALC.Ada_Synth_Anonymous_Type_Decl =>
            return new Synth_Anonymous_Type_Decl;
         when LALC.Ada_Synthetic_Renaming_Clause =>
            return new Synthetic_Renaming_Clause;
         when LALC.Ada_Tagged_Absent =>
            return new Tagged_Absent;
         when LALC.Ada_Tagged_Present =>
            return new Tagged_Present;
         when LALC.Ada_Target_Name =>
            return new Target_Name;
         when LALC.Ada_Task_Body =>
            return new Task_Body;
         when LALC.Ada_Task_Body_Stub =>
            return new Task_Body_Stub;
         when LALC.Ada_Task_Def =>
            return new Task_Def;
         when LALC.Ada_Task_Type_Decl =>
            return new Task_Type_Decl;
         when LALC.Ada_Terminate_Alternative =>
            return new Terminate_Alternative;
         when LALC.Ada_Type_Access_Def =>
            return new Type_Access_Def;
         when LALC.Ada_Type_Decl =>
            return new Type_Decl;
         when LALC.Ada_Un_Op =>
            return new Un_Op;
         when LALC.Ada_Unconstrained_Array_Index =>
            return new Unconstrained_Array_Index;
         when LALC.Ada_Unconstrained_Array_Index_List =>
            return new Unconstrained_Array_Index_List;
         when LALC.Ada_Unconstrained_Array_Indices =>
            return new Unconstrained_Array_Indices;
         when LALC.Ada_Unknown_Discriminant_Part =>
            return new Unknown_Discriminant_Part;
         when LALC.Ada_Until_Absent =>
            return new Until_Absent;
         when LALC.Ada_Until_Present =>
            return new Until_Present;
         when LALC.Ada_Update_Attribute_Ref =>
            return new Update_Attribute_Ref;
         when LALC.Ada_Use_Package_Clause =>
            return new Use_Package_Clause;
         when LALC.Ada_Use_Type_Clause =>
            return new Use_Type_Clause;
         when LALC.Ada_Variant =>
            return new Variant;
         when LALC.Ada_Variant_List =>
            return new Variant_List;
         when LALC.Ada_Variant_Part =>
            return new Variant_Part;
         when LALC.Ada_While_Loop_Spec =>
            return new While_Loop_Spec;
         when LALC.Ada_While_Loop_Stmt =>
            return new While_Loop_Stmt;
         when LALC.Ada_With_Clause =>
            return new With_Clause;
         when LALC.Ada_With_Private_Absent =>
            return new With_Private_Absent;
         when LALC.Ada_With_Private_Present =>
            return new With_Private_Present;
      end case;
   end To_Ada_Node;

end LAL_Adapter.AST_Nodes;
