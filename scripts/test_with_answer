# Test configuration file (see "scripts/rth_run.pl --help" for details)
# This config file simply runs some command specified via the "CMD=" command-line arg, looks at
# its exit status, and verifies that its standard output exactly matches the contents of the
# file specified by the "ANS=" command-line arg.

cmd = ${VALGRIND} ${CMD}
answer = ${ANS}

# Other things that are sometimes convenient to set in the makefile
title = ${TITLE}
disabled = ${DISABLED}
subdir = ${USE_SUBDIR}
require_memory = ${MEMORY}
